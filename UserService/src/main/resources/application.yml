server:
  port: 8083

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/users"
    username: "postgres"
    password: "password"
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
  application:
    name: USER-SERVICE
  config:
    import: optional:configserver:http://localhost:8082
