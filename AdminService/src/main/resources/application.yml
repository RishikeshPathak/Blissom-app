server:
  port: 8084

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/users"
    username: "postgres"
    password: "password"
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
  application:
    name: ADMIN-SERVICE
  config:
    import: optional:configserver:http://localhost:8082
