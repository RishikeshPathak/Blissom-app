server:
  port: 8082

spring:
  application:
    name: CONFIG-SERVER


  cloud:
    config:
      server:
        git:
          uri: https://github.com/RishikeshPathak/blissom-configserver
          clone-on-start: true
